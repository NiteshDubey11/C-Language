//arithemetic operations in lowest possible lines

#include <stdio.h>

int main()
{
   int a,b;
   printf("enter value of A:");
   scanf("%d",&a);
   
   printf("enter value of B:");
   scanf("%d",&b);
   
   printf("The Summation of two numbers are : %d\n",a+b);
   printf("The substraction of two numbers are : %d\n",a-b);
   printf("The Multiplication of two numbers are : %d\n",a*b);
   printf("The Division of two numbers are : %d",a/b);
   

    return 0;
}
